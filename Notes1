Merge VS Append Tables in Power Query
Merge: Combine columns of table  (SQL JOINS)
Append: If column are same combine rows  (SQL UNION)

Process steps: Connect Data-->Filter Data--> Clean Data--> Transform Data-->Combine Data-->clean Up

PIVOT VS UNPIVOT IN POWER QUERY
RESHAPING DATA: Changing the layout of the table without changing the meaning and the values of your data inside the table.
Unpivot: Converting the table structure from human friendly to PowerBi friendly structure. Column to rows
Pivot: Opposite of unpivot Rows to column

DATA MODELING

IN STAR SCHEMA, THE FILTER DIRECTION IS ALWAYS FROM DIMENSION TO THE CENTER(FACT_TABLE) OTHERWISE IT CREATES AMBIGUTY

Active and Inactive relationship: Inactive relationship is one which cause loops, ambiguity between tables and it is not joining on key col just any col ehich has matching data like 2 table having same col eg city.
We cannot use this inactive relationship. 
USERELATIONSHIP(DAX): A DAX FUNCTION THAT TURNS ON AN INACTIVE RELATIONSHIP FOR ONE SPECIFIC CALCULATION.

Grain: The level of detail each row gives is the grain.

PowerBI Row Level Security: Static RLs and Dynamic RLS
